[pytest]
testpaths = tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*
markers =
    slow: marks tests as slow (integration tests with multiple components)
    live: marks tests requiring a live Anthropic API key (skipped in CI)
    unit: marks pure unit tests with no external dependencies
timeout = 60
